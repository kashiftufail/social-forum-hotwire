<%= discussions.count > 0 ? "#{discussions.count} Results" : "No Results"  %> 
<% discussions.each do |discussion| %>
  <br>
  <%= link_to discussion do %>
    <%= highlight(discussion.name, params.dig(:title_search)) %>
  <% end %>
<% end %>