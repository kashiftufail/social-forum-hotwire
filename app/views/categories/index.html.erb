<p style="color: green"><%= notice %></p>

<h1>Categories</h1> <%= link_to "New category", new_category_path ,class: 'btn btn-primary mb-2' if admin?%>

<div id="categories">
  Listing categories below
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      <%= link_to "Show this category", category %>
    </p>
  <% end %>
</div>


